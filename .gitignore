# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
*.log

# OS
.DS_Store

# App runtime data / secrets (never commit vaults or rclone configs)
/run/
*.vaultmesh
rclone.conf

# Downloaded dev rclone binary (the installer bundles it per-OS instead)
/tools/
# Staged rclone binary for packaging (generated by the downloadRclone task)
/app-desktop/resources/

# Native distributions
/app-desktop/build/compose/
